This is a full nodejs app which connects the web app to the mysql database.

See rk-ansible-nodejs-mysql repo for the whole ansible project (on Ubuntu; its the project
ansible-nodejs-2; this repo. also contains the Vagrantfile which creates both the 
webserver and the dbserver.

Ansible also deploys the nodejs project to the github repo here.
